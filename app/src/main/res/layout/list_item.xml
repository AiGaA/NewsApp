<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#e6f2ff">

    <android.support.v7.widget.CardView xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:id="@+id/card_view"
        style="@style/cardViewStyle"
        card_view:cardCornerRadius="4dp">

        <RelativeLayout
            android:id="@+id/text_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="10dp">

            <TextView
                android:id="@+id/newsSection"
                style="@style/newsSectionStyle"
                android:fontFamily="sans-serif-condensed"
                android:textColor="@color/colorAccent"
                android:text="@string/newsSectionPlaceholder" />

            <TextView
                android:id="@+id/newsTitle"
                style="@style/newsTitleStyle"
                android:layout_below="@+id/newsSection"
                android:fontFamily="sans-serif-light"
                android:text="@string/newsTitlePlaceholder" />

            <TextView
                android:id="@+id/newsDate"
                style="@style/newsDateStyle"
                android:text="@string/newsDatePlaceholder" />

        </RelativeLayout>
    </android.support.v7.widget.CardView>
</LinearLayout>